codecov:
  branch: master
  comment: off
  notify:
    require_ci_to_pass: yes
  status:
    project:
      default:
        threshold: 0%
        if_not_found: success
        base: pr
    patch:
      default:
        threshold: 0%
        base: pr
